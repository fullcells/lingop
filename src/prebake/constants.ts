export const PREBAKE_DEFAULT_ASSETS_DIR = "i18n/var";

export const PREBAKE_ASSET_DIRS = {
  translations: "translations",
  annotations: "annotations",
} as const;

export const PREBAKE_METADATA_FILENAME = "metadata.json";
